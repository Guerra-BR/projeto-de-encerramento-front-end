class ComidaClass {
  nome: string
  imagem: string
  descricao: string
  id: number
  constructor(nome: string, imagem: string, descricao: string, id: number) {
    this.imagem = imagem
    this.nome = nome
    this.descricao = descricao
    this.id = id
  }
}

export default ComidaClass
